{
  "client": "wolf-mode.el",
  "client_version": "0.0.1",
  "artifact": "elisp file — clients/emacs/wolf-mode.el, also quoted verbatim in its README",
  "wolf": {
    "min": "0.2.16",
    "max_tested": "0.2.16",
    "pin": "93a5fe504593ca7642b78ba83b4986e7a03cfe71",
    "pin_version_string": "wolf 0.2.16 (wolfgang, pin 93a5fe5)"
  },
  "verified": {
    "date": "2026-09-24",
    "host": "kasumi (linux x86_64), against the ACQUIRED v0.2.16 x86_64-unknown-linux-gnu artifact (sha256 84e30c05…), not a local build — wave 46 puts every cargo invocation there. UNLIKE tl09, the PIN `version` string was read off THAT SAME linux artifact rather than the darwin one, because this lane could run it: `wolf --version` printed \"wolf 0.2.16 (wolfgang, pin 93a5fe5)\" directly. The darwin arm64 archive (sha256 b0431056…) was acquired and hashed member-by-member all the same, and both digests matched the release API's own before anything was unpacked",
    "evidence": [
      "transcripts/emacs/smoke.jsonl was NOT re-captured at this pin and is still at 30731a6 (v0.2.14, tl07's capture) — TWO pins stale now, not one. lspconf --require-server replay SKIPS it by name and exits 0; nothing in this row is earned by a real emacs session at 93a5fe5. wolf-lsp#26 carries the measurement and the reason (no host has all six editors); this lane did not re-capture and does not restate the caveat as resolved",
      "profiles/emacs.json (unchanged; not re-read at this pin — the emacs smoke was not re-driven)",
      "clients/emacs/tests/mode-test.el",
      "lspconf replay + onetruth under this client's derived profile at pin 93a5fe5 (v0.2.16), 2026-09-24 — the 71 scripted transcripts re-recorded and matched (replay: 71 ok, 0 mismatched, 6 skipped; onetruth: 15 samples × 9 profiles, 0 known divergences, zero unfiled). THE SERVER'S ANSWER DID NOT MOVE ANYWHERE: 71 files, 72 insertions, 72 deletions, of which 70 are line 1's wolf_pin and recorded alone, and the 71st — requests/formatting-byte-stable — changes its header and ONE further line, a `c2s` textDocument/didOpen. That is the CLIENT's own payload: the vendored regions.lu moved one line of its `//!` header directive between the tags (`phase: mem` -> `phase: run`, wolf-lang ac256b20, s170's pool lowering), and the formatting RESPONSE is still the empty edit array. Not one s2c line changed in the whole library — measured by classifying every changed line by its `dir` field, not by reading the diff. Both crates the answer comes from are byte-identical across 2e4ca769..93a5fe50 (crates/wolf_query/ and crates/wolf_lsp/), but that was NOT the evidence: tl09 predicted an empty diff from exactly that fact and was wrong, so this claim is the re-record. The library is 77 files and 71 of them were replayed (wolf-lsp#26)"
    ],
    "caveat": "THE CAPTURED SMOKE IS STALE, AND IT IS NOW TWO PINS STALE. transcripts/emacs/smoke.jsonl was captured at 30731a6 (v0.2.14) and replay skips it at 0.2.16, so the part of this range a REAL emacs session earns is two versions behind the rest. The scripted suite — which covers every rung the smoke covers, through this client's own derived profile — did run at 93a5fe5. wolf-lsp#26."
  },
  "warning_surface": "none — the mode is a copy-pasteable snippet, and a snippet that phones a version check is a snippet nobody pastes"
}
