{
  "client": "helix config fragment",
  "client_version": "0.0.1",
  "artifact": "TOML fragment — clients/helix/languages.toml, copied into the user's config",
  "wolf": {
    "min": "0.2.15",
    "max_tested": "0.2.15",
    "pin": "2e4ca769b396219585a07ff18492529c944672d9",
    "pin_version_string": "wolf 0.2.15 (wolfgang, pin 2e4ca76)"
  },
  "verified": {
    "date": "2026-09-17",
    "host": "kasumi (linux x86_64), against the ACQUIRED v0.2.15 x86_64-unknown-linux-gnu artifact (sha256 dd28150c…), not a local build — wave 45 puts every cargo invocation there; the PIN `version` string was read off the ACQUIRED darwin arm64 artifact (sha256 166004ee…), both digests matched against the release API's own",
    "evidence": [
      "transcripts/helix/smoke.jsonl was NOT re-captured at this pin and is still at 30731a6 (v0.2.14, tl07's capture). lspconf --require-server replay SKIPS it by name and exits 0; nothing in this row is earned by a helix session at 2e4ca76. wolf-lsp#26 carries the measurement and why neither host this wave could drive all six editors",
      "profiles/helix.json (unchanged; not re-read at this pin — the helix smoke was not re-driven)",
      "cargo xtask helix-health",
      "lspconf replay + onetruth under this client's derived profile at pin 2e4ca76 (v0.2.15), 2026-09-17 — the 71 scripted transcripts re-recorded and matched (replay: 71 ok, 0 mismatched; onetruth: 15 samples × 9 profiles, 0 known divergences, zero unfiled). The re-record is HEADER-ONLY across 70 of the 71 and a TWO-TOKEN body diff in the 71st: 71 files, 73 insertions, 73 deletions, of which 70 are line 1's wolf_pin and recorded and the other three are annotate/semanticTokens-error's header and its two semanticTokens answers, which gain IoErrors as `type` at 0-based line 13 column 22 and ConfigErrors as `type` at line 15 column 27 — wolf-lang#379's fix (b2ad629, s165), whose closing comment predicted both coordinates exactly before this lane measured them. No other response body moved, and the negative half of that witness (rows/error_alias_ident.lu, seven identifiers spelled `error`) is byte-identical. UNLIKE tl07 AND tl03, replay now SKIPS SIX FILES: the captured editor smokes are still at 30731a6 and were NOT re-captured at this pin, so the library is 77 files and 71 of them were replayed (wolf-lsp#26)"
    ],
    "caveat": "THE CAPTURED SMOKE IS STALE. transcripts/helix/smoke.jsonl was captured at 30731a6 (v0.2.14) and replay skips it at this pin, so the part of this range that a REAL helix session earns is one version behind the rest. The scripted suite — which covers every rung the smoke covers, through this client's own derived profile — did run at 2e4ca76. wolf-lsp#26."
  },
  "warning_surface": "none — a TOML fragment cannot run code; docs/COMPAT.md is the whole statement"
}
