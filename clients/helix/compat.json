{
  "client": "helix config fragment",
  "client_version": "0.0.1",
  "artifact": "TOML fragment — clients/helix/languages.toml, copied into the user's config",
  "wolf": {
    "min": "0.2.14",
    "max_tested": "0.2.14",
    "pin": "30731a6509443ade03724707345da9ceb86a88bf",
    "pin_version_string": "wolf 0.2.14 (wolfgang, pin 30731a6)"
  },
  "verified": {
    "date": "2026-09-14",
    "host": "local (macos arm64), against the ACQUIRED v0.2.14 darwin artifact (sha256 80407e31…), not a local build",
    "evidence": [
      "transcripts/helix/smoke.jsonl was RE-CAPTURED at this pin (tl07): helix 25.07.1 driven through a pty by the documented key table, 19 records, two consecutive runs byte-identical. A field-by-field comparison over every record finds exactly two changed fields, `recorded` and `wolf_pin` — HEADER-ONLY, but only once the capture was driven from a checkout NAMED wolf-lsp: helix puts the workspace root's basename in `workspaceFolders[0].name`, the normalizer elides the path and not the name, and a first run from `/private/tmp/tl07` moved that one field (tl07's fourth driver trap, clients/helix/README.md). `lspconf --require-server replay` does not skip it (ok, 9 records matched)",
      "profiles/helix.json (re-read at 25.07.1 — byte-identical to the capture, no re-derivation needed)",
      "cargo xtask helix-health",
      "lspconf replay + onetruth under this client's derived profile at pin 30731a6 (v0.2.14), 2026-09-14 — 70 scripted transcripts re-recorded and matched, plus a 71st written at tl07 (annotate/semanticTokens-error, wolf-lsp#22); with the six captured smokes re-captured at tl07 the whole library is 77 files and `replay` skips none of them, 15 samples × 9 profiles with zero divergences (the 14th and 15th are rows/error_alias_union.lu and rows/error_alias_ident.lu, vendored here). The re-record is HEADER-ONLY across 69 of the 70 files and a ONE-TOKEN body diff in the 70th: 70 files, 72 insertions, 72 deletions, of which 69 are line 1's `wolf_pin` and `recorded` and the other three are annotate/semanticTokens-then's header and its two semantic-token answers, each gaining exactly the contextual `then` at line 12 column 20 as `keyword` — wolf-lang#356's fix (`928f9d9`), the one server change between v0.2.12 and v0.2.14 that any vendored sample can see. No other response body moved"
    ]
  },
  "warning_surface": "none — a TOML fragment cannot run code; docs/COMPAT.md is the whole statement"
}
