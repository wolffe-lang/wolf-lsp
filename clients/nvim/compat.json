{
  "client": "wolf.nvim",
  "client_version": "0.0.1",
  "artifact": "plugin tree — clients/nvim/, published as the generated wolf.nvim mirror",
  "wolf": {
    "min": "0.2.12",
    "max_tested": "0.2.12",
    "pin": "a7f517e1f667f142ffaf61b57aa2bff06e32b520",
    "pin_version_string": "wolf 0.2.12 (wolfgang, pin a7f517e)"
  },
  "verified": {
    "date": "2026-09-12",
    "host": "local (macos arm64), against the ACQUIRED v0.2.12 darwin artifact (sha256 6be493a9…), not a local build",
    "evidence": [
      "transcripts/nvim/smoke.jsonl was RE-CAPTURED at this pin (tl03, wolf-lsp#14): NVIM v0.12.5 headless with the documented shim, 7/7 smoke.lua assertions passing while recording, 32 records. THREE fields moved across all 33 — `recorded`, `wolf_pin`, and `clientInfo.version` (0.12.5 -> 0.12.5+v0.12.5, the client's own build stamp, because Homebrew's neovim will not start on this box and the official release tarball was used instead). Every server response body is byte-identical. `lspconf --require-server replay` no longer skips it (ok, 15 records matched)",
      "profiles/nvim.json (re-read at v0.12.5 — unchanged since le07's re-derivation)",
      "clients/nvim/tests/plugin_spec.lua",
      "lspconf replay + onetruth under this client's derived profile at pin a7f517e (v0.2.12), 2026-09-12 — 69 scripted transcripts re-recorded and matched, plus a 70th written at tl02 (annotate/semanticTokens-then); with the six captured smokes re-captured at tl03 the whole library is 76 files and `replay` skips none of them, 13 samples × 9 profiles with zero divergences (the 13th is grammar/if_then_ident.lu, vendored here). The re-record is a HEADER-ONLY diff across all 69 files: 69 insertions, 69 deletions, every one of them line 1's `wolf_pin` and `recorded`, so no response body moved between v0.2.5 and v0.2.12"
    ]
  },
  "warning_surface": ":checkhealth wolf"
}
