{
  "client": "zed_wolf",
  "client_version": "0.0.1",
  "artifact": "wasm32-wasip2 component + manifest — clients/zed/",
  "wolf": {
    "min": "0.2.16",
    "max_tested": "0.2.16",
    "pin": "93a5fe504593ca7642b78ba83b4986e7a03cfe71",
    "pin_version_string": "wolf 0.2.16 (wolfgang, pin 93a5fe5)"
  },
  "verified": {
    "date": "2026-09-24",
    "host": "kasumi (linux x86_64), against the ACQUIRED v0.2.16 x86_64-unknown-linux-gnu artifact (sha256 84e30c05…), not a local build — wave 46 puts every cargo invocation there. UNLIKE tl09, the PIN `version` string was read off THAT SAME linux artifact rather than the darwin one, because this lane could run it: `wolf --version` printed \"wolf 0.2.16 (wolfgang, pin 93a5fe5)\" directly. The darwin arm64 archive (sha256 b0431056…) was acquired and hashed member-by-member all the same, and both digests matched the release API's own before anything was unpacked",
    "evidence": [
      "cargo build --target wasm32-wasip2 (CI job `zed-extension`)",
      "cargo xtask config-check",
      "lspconf replay + onetruth at pin 93a5fe5 (v0.2.16), 2026-09-24 — under the OTHER clients' derived profiles; Zed still has no recorded session of its own. The suite itself: replay 71 ok, 0 mismatched, 6 skipped; onetruth 15 samples × 9 profiles, 0 known divergences, zero unfiled. The server's answer did not move at this pin — 71 files, 72 insertions, 72 deletions, 70 of them line 1 alone, and the 71st's only body line is a `c2s` didOpen carrying the re-vendored regions.lu (`phase: mem` -> `phase: run`, wolf-lang ac256b20). Not one s2c line changed."
    ],
    "caveat": "NO SESSION HAS EVER BEEN RECORDED. Zed has never been run against this extension by CI or by hand (docs/MATRIX.md). Its row inherits the range from the pin the other clients' scripted suites were verified at; a Zed session did not earn it — profiles/zed.json and transcripts/zed/smoke.jsonl are still owed. Read the row as 'the wasm builds and the config is statically consistent at this pin', which is the whole T2 claim. (tl11 briefly wrote the six-smokes caveat here by bulk edit and caught it: there is no transcripts/zed/smoke.jsonl to be stale, and a row claiming a capture that does not exist is worse than the gap it papers over.)"
  },
  "warning_surface": "none — the extension's wasm entry point is `language_server_command`, which Zed calls before any place a notification could be raised"
}
