{
  "client": "zed_wolf",
  "client_version": "0.0.1",
  "artifact": "wasm32-wasip2 component + manifest — clients/zed/",
  "wolf": {
    "min": "0.2.15",
    "max_tested": "0.2.15",
    "pin": "2e4ca769b396219585a07ff18492529c944672d9",
    "pin_version_string": "wolf 0.2.15 (wolfgang, pin 2e4ca76)"
  },
  "verified": {
    "date": "2026-09-17",
    "host": "kasumi (linux x86_64), against the ACQUIRED v0.2.15 x86_64-unknown-linux-gnu artifact (sha256 dd28150c…), not a local build — wave 45 puts every cargo invocation there; the PIN `version` string was read off the ACQUIRED darwin arm64 artifact (sha256 166004ee…), both digests matched against the release API's own",
    "evidence": [
      "cargo build --target wasm32-wasip2 (CI job `zed-extension`)",
      "cargo xtask config-check",
      "lspconf replay + onetruth at pin 2e4ca76 (v0.2.15), 2026-09-17 — under the OTHER clients' derived profiles; Zed still has no recorded session of its own"
    ],
    "caveat": "NO SESSION HAS EVER BEEN RECORDED. Zed has never been run against this extension by CI or by hand (docs/MATRIX.md). Its row inherits the range from the pin the other clients' scripted suites were verified at; a Zed session did not earn it — profiles/zed.json and transcripts/zed/smoke.jsonl are still owed. Read the row as 'the wasm builds and the config is statically consistent at this pin', which is the whole T2 claim."
  },
  "warning_surface": "none — the extension's wasm entry point is `language_server_command`, which Zed calls before any place a notification could be raised"
}
