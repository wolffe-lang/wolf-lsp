# Compatibility

**Filled by ls07.** Versioning is keyed to wolf releases, not to this repo's
own cadence: an extension version encodes the minimum `wolf` it requires, and
refuses to start against an older toolchain with an actionable message.

Until then the only compatibility fact this repo has is its pin:
`vendor/upstream/PIN`.
