# The wolf-lang commit this repo is keyed to. A bump is its own commit, with
# the re-vendored data, and nothing else (vendor/README.md).
#
# tl11 pins the RELEASE TAG `v0.2.16`, which is an ANNOTATED tag peeling to the
# commit below — tl09 pinned v0.2.15, tl07 pinned v0.2.14, tl02 pinned v0.2.12
# and le04..le08 pinned v0.2.1..v0.2.5 the same way; nothing here requires a
# tag, and the next bump may be a trunk sha again. Note the peel:
# `git rev-parse v0.2.16` answers the TAG object, not the commit —
# `v0.2.16^{commit}` is what belongs here, and reading the unpeeled form would
# pin a sha no `wolf --version` will ever print.
commit = 93a5fe504593ca7642b78ba83b4986e7a03cfe71
# The exact `wolf --version` string that commit produces (first line; the
# binary also prints a second line naming its lupin pairing — 0.1.38 at pin
# `2e4ca76`, up from 0.1.37 at pin `41695e7`). `lspconf doctor` fails on a
# mismatch: a stale local `wolf` producing green transcripts is the failure
# mode the pin exists to prevent.
#
# A NOTE ON THAT SECOND LINE, AND A CORRECTION TO THE ONE THIS FILE CARRIED AT
# THE LAST PIN. The pairing sha is **a wolf-LANG commit, not a wolf-interp
# one** — `crates/wolf_driver/PAIRING` calls it "the conformance pin that
# lupin release itself declares", i.e. the wolf this lupin says it conforms
# to. The 0.2.15 text here said `41695e7` was "NOT reachable from any
# wolf-interp branch" and sent a reader to fetch a wolf-interp tag; both
# halves name the wrong repository. Measured: `41695e78` is a commit in
# **wolf-lang**, unreachable from wolf-lang trunk, preserved as
# `refs/tags/lupin-0.1.37-conformance-pin` in **wolf-lang**; wolf-interp has
# no object by that name at all, so the advice as written could only ever
# produce the "not a valid object" it was warning about. (wave-46's rule: a
# sha cited as another repository's pin resolves THERE, and the check names
# the repository the text names.)
#
# AND THE WART IS GONE AT THIS PIN. 0.2.16's pairing pin is `2e4ca76` — the
# v0.2.15 release tag's own commit, an ancestor of wolf-lang trunk, resolvable
# in any clone with no extra fetch. wolf-lang `8514c27e` says so in its own
# subject: "the released tag itself, for the first time". The unreachable-pin
# hazard this paragraph existed to explain does not apply to 0.2.16.
#
# D57 decides the shape. A build is a release build only when the builder
# stamps BOTH `WOLF_COMMIT=<short sha>` and `WOLF_RELEASE=v{crate version}`,
# and the second is granted only when that exact tag points at HEAD. This pin
# is at v0.2.16, so a release build prints the BARE version and the pin clause.
# An unstamped build still prints `+dev.unknown` — the string any trunk build
# of the same crate version prints, which is exactly the stale-binary hole —
# so the pinned string remains a stamped build's, and `cargo xtask dist` in
# the submodule is the builder that computes the stamp (README §Running the
# server lane locally; its plain-cargo recipe cannot grant the release half).
#
# `xtask compat`'s `version_number` reads the number out of this string; it
# learned semver build metadata at le03 and takes the bare tag form
# unchanged — `0.2.16`, no `+` to strip. Asserted in xtask/src/compat.rs.
#
# SEVEN HEX DIGITS FOR THE EIGHTH PIN RUNNING, and the line below was READ OFF
# THE ACQUIRED ARTIFACT rather than off any clone on this box — the
# x86_64-unknown-linux-gnu archive of release 395302343, sha256
# 84e30c05e56b89fde2fe6e27eec8f7cec0a6aae6e1464a2993fcfc87a7a1829e (the
# release asset's own digest, matched after download), whose `wolf --version`
# prints exactly the string below. That matters because wolf-lang#199 is still
# open: D57's pin clause is clone-dependent, `git rev-parse --short` sizes its
# AUTO abbreviation to the PACKED object count of the clone it runs in, and the
# step is at 16,384 packed objects (2^14) — measured at le08 against three
# scratch repositories answering 7 / 8 / 8 hex digits (git 2.50.1). A locally
# built submodule binary on a big clone can therefore print eight digits and
# fail `doctor` here, and that refusal is the pin doing its job. The remedy is
# upstream — `--short=7`, or `core.abbrev`, in whatever computes the stamp.
# See docs/COMPAT.md.
version = "wolf 0.2.16 (wolfgang, pin 93a5fe5)"
# Does `wolf lsp` exist at this commit? YES, and it ANNOTATES, as it has since
# the v0.2.5 pin. wolf-lang s52 landed the subcommand, s133 added definition,
# references and rename, and s134 added `signatureHelpProvider`,
# `semanticTokensProvider` and `inlayHintProvider` to the `initialize` answer.
# Those flips are what light every server-dependent lane: record, replay,
# onetruth, bench, and the encoding, cancellation, navigation and fuzz suites.
#
# THE ARCHIVE PAIR, HASHED BY NAME. Both archives were downloaded and both
# matched the API's own asset digest byte for byte:
#
#   wolf-0.2.16-x86_64-unknown-linux-gnu.tar.gz  84e30c05…  70,516,905 bytes
#   wolf-0.2.16-aarch64-apple-darwin.tar.gz      b0431056…  13,911,651 bytes
#
# Every member is hashed BY NAME, never "the first executable in the archive",
# and the reason is worth repeating because this pin reproduces it a SEVENTH
# time. `_wolf` sorts before `wolf` in ASCII and is NOT a second binary: it is
# the zsh completion script, 1,913 bytes, `#compdef wolf`, shipped with the
# executable bit set — and its digest is `2d1e4801…` in BOTH archives here,
# the same constant sc51 found in all six archives it hashed across 0.2.13,
# 0.2.14 and 0.2.15 on two platforms. A check that hashes "the first binary it
# finds" reads that same constant at every pin on every platform: not merely
# wrong, UNFALSIFIABLE — it cannot go red at any release. The two members that
# actually move:
#
#   wolf                 b53b5328…  271,974,592   linux x86-64
#   wolf                 88001334…   14,726,120   darwin arm64
#   libwolf_rt.a         c4c5f670…    38,246,706  linux x86-64
#   libwolf_rt.a         b804455d…    27,436,696  darwin arm64
#   wolf-cimport-worker  cf2c47a9…    10,769,504  linux x86-64
#   wolf-cimport-worker  78812167…       702,216  darwin arm64
#
# and the six that do not (identical across both platforms): `_wolf`
# 2d1e4801…, `wolf.1` afcf4f7c…, `wolf.bash` 29eb2d75…, `wolf.fish`
# 51a1b6a8…, `LICENSE` 3972dc97…, `LICENSE-EXCEPTION` a0eec20d…, `README.md`
# 8e148142…. Ten members per archive, both archives, nothing unaccounted for.
#
# THE ARCHIVE IS WHOLE AT THIS TAG, AND — FOR THE FIRST TIME SINCE v0.2.3 —
# THE PAGE IS CLEAN. Release id 395302343 is published (`draft=false`,
# `published_at` 2026-09-24T03:55:26Z), targets `trunk`, and carries FOUR
# assets:
#   wolf-0.2.16-aarch64-apple-darwin.tar.gz       (13,911,651 bytes)
#   wolf-0.2.16-aarch64-unknown-linux-gnu.tar.gz  (59,369,137 bytes)
#   wolf-0.2.16-x86_64-pc-windows-msvc.tar.gz     (11,980,810 bytes)
#   wolf-0.2.16-x86_64-unknown-linux-gnu.tar.gz   (70,516,905 bytes)
# — the same four-triple set v0.2.3 first carried, so le06's acquire step
# needs no change and all three tier-1 runners stay covered.
#
# **EXACTLY ONE RELEASE CARRIES THE TAG `v0.2.16`.** The upstream wart this
# file recorded at six consecutive pins — a tag with the published release
# plus THREE empty drafts created in the same second, because each archive job
# created-or-got the release and three lost — does NOT reproduce. wolf-lang#226
# is fixed at this cut: the draft is created once, in its own job, the archives
# upload to it by id, and publishing refuses unless exactly one release carries
# the tag. Queried the whole release list for `tag_name == "v0.2.16"`: one row,
# `draft=false`, four assets. The paragraph stays here in its corrected form
# because the six reproductions are the reason the fix is worth recording.
#
# THE LINUX ARCHIVES HAVE A GLIBC FLOOR NOW, AND IT IS 2.35 (wolf-lang#447).
# 0.2.15's linux archives were built on Ubuntu 24.04 and imported GLIBC_2.39,
# so they did not start on 22.04 at all. Measured here on the acquired 0.2.16
# x86-64 archive: the highest GLIBC symbol version the binary imports is
# **GLIBC_2.34**, comfortably under the stated 2.35 floor, and it runs on this
# lane's build host (glibc 2.44). Nothing in this repo pins a runner's libc,
# but a CI runner older than Ubuntu 22.04 would now be the constraint.
#
# WHAT v0.2.15..v0.2.16 ADDS THAT THIS REPO CAN SEE — measured, not swept.
# 148 commits, 125 files, +14,202 −2,028, and eight merged lanes. The surface
# this repo reads moves in exactly three places, and one of them is a place no
# gate in this repository can see.
#
# THE SERVER'S CODE DOES NOT MOVE AT THIS PIN, AND THIS TIME NEITHER CRATE
# MOVES. The whole of `crates/wolf_query/` — where the semantic-token walk
# lives, in `src/annotate.rs` — AND the whole of `crates/wolf_lsp/` are
# BYTE-IDENTICAL across the span; `git diff 2e4ca769 93a5fe50 --` is empty for
# each. At the last pin `wolf_lsp` was one test file different; here it is
# nothing at all.
#
# BUT A BYTE-IDENTICAL QUERY CRATE IS STILL NOT A PREDICTION OF A
# BYTE-IDENTICAL ANSWER — tl09 learned that the hard way and this lane did not
# repeat the mistake, so the transcript claim below is a MEASUREMENT of all 71
# scripted sessions re-recorded at this pin, not an inference from a diffstat.
# (The "THE TRANSCRIPTS" block below is that measurement.)
#
# `crates/wolf_sema/src/prelude.rs` grows by 23 lines and TWO OF THEM ARE TYPE
# NAMES — the first prelude NAMES added since s158's `range`, and the first
# time in this file's history that the answer to "did the type list move?" is
# yes. `PRELUDE` gains **`Scope`** and **`Proc`** (s170, wolf-lang#316, BACKLOG
# B21's ruling): `[conc.task.scope]` has said since D16 that "a function that
# spawns into its caller's scope takes the handle as a parameter", and until
# this release there was no way to write that parameter — `Scope`/`Proc` were
# E0301 and `scope`/`proc` were E0206. `fn fan_out(s: Scope)` and
# `fn watch(p: Proc[int])` are programs now. `BUILTIN_TYPES` (seventeen prims)
# and `PRELUDE_TYPE_ONLY` (still just `range`) are byte-identical at the two
# commits — extracted and hashed separately rather than inferred from the
# file's diffstat, because a file that grew is not the same claim as a list
# that did not. The other 14 lines are one new `#[test]`.
#
# (The contract for this lane said 22 lines and attributed some of them to
# s173's pool names. Measured: `git diff --numstat` is `23 0`, and NOT ONE
# pool name is added — `Pool`, `Mutex` and `channel` appear in the diff only
# as context, present at 2e4ca769 already. s173's pool work is all in `wir`
# and `wolf_rt`. Drift reported, not absorbed.)
#
# THE SPEC DIFF IS SIX FILES, +347 −5, and TWO of them are vendored here.
#
# `spec/anchors.json`: 524 → 539, FIFTEEN ADDED AND NONE DROPPED. Diffed as
# KEY SETS IN BOTH DIRECTIONS (new−old and old−new) and for RETARGETING (a key
# whose file changed), not by comparing two counts — wolf-lang#177's lesson is
# that a regeneration can add and drop in the same move and a count hides it.
# All three answers: 15, 0, 0. NOTE for anyone re-running it: the file's TOP
# LEVEL has two keys, `anchors` and `version`, so a naive count of the JSON
# prints **2**; the set to diff is `.anchors`. The fifteen:
#
#   [conc.proc.arg] [conc.proc.handle] [conc.proc.join]   s170  #312/#316/#110
#   [conf.exit] .class .collide .refused .static .test    s169  exit codes
#   [mem.region.imm.ret] [mem.region.root]                s168
#   [proto.cmp.pass] [proto.record.pass] [proto.record.trap]  s169
#   [type.err.alias.qualified]                            s175  #434
#
# Reconciled against the release CHANGELOG clause by clause: every clause the
# notes name as LANDED appears above, and nothing above is unaccounted for.
# The clauses the notes discuss that are NOT here — `[type.list.lit.elem]`,
# `[mem.str.ws]`, `[mem.shared.handle.2]`/`.3` — are RE-WORDINGS of anchors
# that already existed, which is why fifteen is smaller than a reader of the
# notes would guess and why the diff had to be over KEYS and not over the file.
#
# THE TYPE-NAMES GATE IS GREEN AND IT IS BLIND, AND THAT IS THIS PIN'S FINDING.
# The gate (wolf-lsp#24, `xtask/src/type_names.rs`) reads the SECOND SEGMENT of
# every `type.*` anchor as its candidate set. Diffed both ways across the two
# tags that set is EIGHTEEN WORDS ON EACH SIDE, zero added, zero dropped —
# `byte char closure comb err float fn generic interp list map method numlit
# range row str trait unit`. The one new `type.*` anchor is
# `type.err.alias.qualified` and its second segment `err` was already there.
# So the gate asks the 0.2.16 binary exactly the questions it asked the 0.2.15
# one, and passes.
#
# Meanwhile `Scope` and `Proc` — two names the compiler resolves in type
# position at this pin and refused at the last one — are NOT in the candidate
# set and cannot be, because their clauses are anchored `conc.proc.handle` and
# `[conc.task.scope]`: **`conc.*`, not `type.*`**. This is verbatim the first
# bullet of `type_names.rs`'s own "What the gate cannot see" — *a builtin added
# upstream without a two-segment `type.<name>` anchor of its own spelling* —
# which has been a hypothetical since wolf-lsp#24 was written and is LIVE for
# the first time here. The gate stays green while the set it guards is two
# names short. It is filed rather than absorbed; see docs/PIN-0216.md §4.
#
# `spec/grammar.ebnf` DID NOT CHANGE, checked two ways rather than one.
# `git diff --quiet 2e4ca769 93a5fe50 -- spec/grammar.ebnf` exits **0**, AND
# the blob sha is `4b2ed9939875a8a7d65924449fbd2ba29a3fcd56` on both sides —
# the second check is the stronger one, because a diff can be quieted by a
# filter and a blob sha cannot. So no production moved, the word-terminal set
# stays at 75 and the symbolic set does not move, `reserved_kw` is
# byte-identical at fifty names, and NOTHING derived from the EBNF is
# regenerated at this bump: a commit of identical bytes would be a claim that
# something moved. tl09's five `FMT_TYPE` contextual rulings were the previous
# release's and do not recur.
#
# ONE VENDORED SAMPLE DID MOVE, and it is the first time since le08 that any
# has. Measured TWO WAYS (sha256 of the working files, and `git rev-parse
# <tag>:corpus/<path>` on both sides — fifteen blob-sha pairs): FOURTEEN
# identical, ONE different. `regions.lu` changes by exactly one line, and it
# is in the `//!` HEADER DIRECTIVE, not the program:
#
#     -//! phase: mem
#     +//! phase: run
#
# wolf-lang `ac256b20` (s170, "Pool[T] and handle T lower — three refusals
# narrowed, three witnesses mem -> run"): the pool now lowers, so this witness
# runs where it used to stop at the mem tier. The program below the header is
# byte-for-byte what it was. It matters here anyway, because `regions.lu` is
# the FORMATTING subject — "the largest canonical `wolf fmt` output in the
# set, so a formatting response is a real byte-for-byte claim" — and a
# formatting response echoes the document, header comment included. So
# `requests/formatting-byte-stable` moves at this pin for an INPUT reason and
# not because the server changed its mind, and the two causes are kept apart
# in the transcript block below.
#
# THE TRANSCRIPTS, MEASURED. All 71 scripted sessions were re-recorded at this
# pin against the acquired 0.2.16 linux binary. `71 files changed, 72
# insertions(+), 72 deletions(-)`:
#
#   * SEVENTY are HEADER-ONLY — line 1's `wolf_pin` and `recorded`, nothing
#     else.
#   * ONE has a second changed line: `requests/formatting-byte-stable`. That
#     line is a **`c2s` `textDocument/didOpen`** — the CLIENT's own payload,
#     carrying the re-vendored `regions.lu` with `phase: mem` become
#     `phase: run`. The formatting RESPONSE did not move: it is still the empty
#     edit array the transcript exists to pin.
#
# So **NOT ONE `s2c` LINE CHANGED IN THE WHOLE LIBRARY**. The server's answer
# to every request is byte-identical at 0.2.15 and 0.2.16. That was measured by
# classifying every changed line by its `dir` field, not by eyeballing a diff,
# because "70 header-only and one body diff" is the same shape whether the
# server moved or the document did, and the two are not the same claim.
# `replay` at the new pin: **71 ok, 0 mismatched, 6 skipped, exit 0**.
# `onetruth`: 15 samples x 9 profiles, 0 known divergences, zero unfiled.
#
# THIS LANE PREDICTED ZERO MOVERS AND WAS WRONG BY ONE, and the reason is worth
# more than the number. The prediction walked every behaviour change in the
# release against the fifteen vendored sample DOCUMENTS — #444's `move`, #312's
# spawn, #316's handles, #400's fn-parameter, #391/#392's region charging,
# s169's exit codes, `[type.list.lit.elem]`, `[mem.str.ws]` — and every one of
# those calls was right. What it never checked was whether the INPUTS had
# moved. tl09 measured that explicitly ("fifteen blob-sha pairs, fifteen
# matches") and this lane assumed it, so the one thing that moved is the one
# thing that was not looked at. The two named fallback candidates —
# `annotate/semanticTokens-error` (for s175's rewrite of error-row entry
# resolution) and `requests/document-symbol` / `hover-nothing-there` (for
# `errors.lu`'s `io.Error`, the only qualified row entry in the sample set) —
# BOTH held, which is itself the useful answer: wolf-lang#434 is a strict
# extension on the same-module single-segment case, and an unresolvable `io.`
# prefix still falls through to a tag.
#
# The lesson, stated so the next bump can use it: **a pin bump has two
# independent sources of transcript movement — what the server answers, and
# what the vendored documents say — and a prediction about one is not a
# prediction about the other.** The 0.2.15 bump moved the first and not the
# second; this one moved the second and not the first.
#
# THE SIX CAPTURED EDITOR SMOKES ARE STILL NOT RE-CAPTURED, and they are now
# TWO pins stale — still at `30731a6` (v0.2.14). `lspconf replay` skips them by
# name and exits 0, so the library is 77 files and 71 of them are replayed
# here. wolf-lsp#26 carries the measurement and the reason (no host has all
# six editors); this lane changes nothing about it and leaves the caveat in
# place rather than quietly re-stating it as current.
serves_lsp = true
